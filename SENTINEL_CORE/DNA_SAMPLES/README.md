# DNA_SAMPLES — Paires (Input, Output Parfait)

Analogie fondatrice :
    Input  = l'equation
    Output = la reponse connue
    Code   = la methode que SENTINEL cherche

Ces paires permettent a SENTINEL B8 de calibrer les parametres
par induction plutot que par supposition.
