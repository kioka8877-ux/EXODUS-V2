# ╔══════════════════════════════════════════════════════════════════════════════╗
# ║           TRANSMUTATION ENGINE V2 — Python Dependencies                      ║
# ╠══════════════════════════════════════════════════════════════════════════════╣
# ║  ZÉRO EMOCA — Emotional Intent Transfer via expression_schema.py            ║
# ║  Blender 4.0 headless installé séparément sur le Drive                      ║
# ╚══════════════════════════════════════════════════════════════════════════════╝

# Aucune dépendance externe requise pour le pipeline principal.
# expression_schema.py et facial_extractor.py sont pure Python stdlib.
# blender_fusion.py utilise bpy (fourni par Blender headless).
# scipy reste disponible pour smoothing.py (module legacy, non utilisé en V2).

# Legacy (optionnel, pour compatibilité smoothing.py)
scipy>=1.10.0
